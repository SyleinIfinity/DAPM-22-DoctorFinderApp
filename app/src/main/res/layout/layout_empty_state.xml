<?xml version="1.0" encoding="utf-8"?>
<!--
    Empty State: Bạn chưa theo dõi bác sĩ nào
    File: layout_empty_state.xml
    Dùng khi danh sách theo dõi trống.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/card_margin_h"
    android:layout_marginEnd="@dimen/card_margin_h"
    android:layout_marginBottom="@dimen/spacing_xl"
    app:cardCornerRadius="@dimen/card_radius"
    app:cardElevation="3dp"
    app:cardBackgroundColor="@color/white"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/spacing_lg"
        android:gravity="center_vertical">

        <!-- Illustration icon -->
        <FrameLayout
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:background="@drawable/ic_heart">

            <ImageView
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_clipboard_heart"
                android:tint="@color/primary"
                android:contentDescription="Chưa theo dõi" />

        </FrameLayout>

        <!-- Text + CTA -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="@dimen/spacing_md">

            <!-- Title -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/empty_title"
                android:textColor="@color/text_primary"
                android:textSize="@dimen/text_md"
                android:fontFamily="sans-serif-medium" />

            <!-- Description -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_xs"
                android:text="@string/empty_desc"
                android:textColor="@color/text_secondary"
                android:textSize="@dimen/text_sm"
                android:lineSpacingMultiplier="1.4" />

            <!-- CTA Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnExploreDoctors"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:layout_marginTop="@dimen/spacing_md"
                android:text="@string/kham_pha_bac_si"
                android:textColor="@color/white"
                android:textSize="@dimen/text_sm"
                android:fontFamily="sans-serif-medium"
                style="@style/Widget.MaterialComponents.Button"
                app:backgroundTint="@color/primary"
                app:cornerRadius="24dp" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
